[project]
name = "bonsai-node"
version = "0.1.0"
description = "ComfyUI custom node that manages a Bonsai llama-server lifecycle"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0.0",
    "sentence-transformers>=3.0.0",
    "torch>=2.2.0",
]
